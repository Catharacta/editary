# Editary - 開発ロードマップ

## フェーズ 1: プロジェクト基盤の構築 🏗️
> **目標**: ElectroBunプロジェクトを初期化し、開発環境を整備する

- [ ] ElectroBunプロジェクトを `bunx electrobun init` で初期化する
- [ ] React + Vite + TypeScript のフロントエンド環境をセットアップする
- [ ] pnpm で依存関係を管理する
- [ ] TailwindCSS を導入し、ネオブルータリズムのデザイントークンを定義する
- [ ] ElectroBun のフレームレスウィンドウ設定（カスタムタイトルバー用）
- [ ] `.gitignore` の設定
- [ ] ESLint / Prettier の設定

**完了条件**: `bun start` でElectroBunウィンドウが起動し、React画面が表示される

---

## フェーズ 2: コアエディターの実装 ✍️
> **目標**: Tiptapベースのリアルタイム Markdown エディターを実装する

- [ ] Tiptap エディターと依存パッケージをインストールする
- [ ] 基本的な Markdown 拡張機能を構成する
  - Document, Text, Paragraph, Heading (H1〜H6)
  - Bold, Italic, Strike
  - BulletList, OrderedList, ListItem
  - CodeBlock, Code (inline)
  - Blockquote, HorizontalRule
  - Link, Image
- [ ] Markdown 構文入力 → 即座にフォーマット変換される動作を実装する
  - `## ` → H2 見出し
  - `**text**` → 太字
  - `- ` → 箇条書き
  - `` `code` `` → インラインコード
  - `> ` → 引用
- [ ] エディター領域にネオブルータリズムのスタイルを適用する

**完了条件**: エディター上で Markdown を入力するとリアルタイムで装飾が適用される

---

## フェーズ 3: IPC通信とファイルシステム連携 📁
> **目標**: Bun (メインプロセス) と React (Webview) 間のファイル操作を実装する

- [ ] ElectroBun の IPC チャンネルを設計・実装する
- [ ] `openFolder`: OS のネイティブフォルダ選択ダイアログ
- [ ] `readDirectory`: `.md` ファイル一覧の再帰的取得
- [ ] `readFile`: ファイル内容の読み込み
- [ ] `writeFile`: ファイル内容の保存（上書き保存 & 新規保存）
- [ ] `createFile`: 新規 `.md` ファイルの作成
- [ ] エラーハンドリング（アクセス権限、ファイル競合など）

**完了条件**: Webview から Bun の IPC を通じて `.md` ファイルの CRUD 操作ができる

---

## フェーズ 4: UI の組み立て 🎨
> **目標**: ネオブルータリズムのサイドバー、タイトルバー、レイアウトを実装する

- [ ] グローバルレイアウト (サイドバー + タイトルバー + エディター) の構築
- [ ] カスタムタイトルバーの実装（最小化、最大化、閉じるボタン）
- [ ] ネオブルータリズムのサイドバーコンポーネント
  - フォルダ/ファイルツリーの表示
  - フォルダの展開/折りたたみ
  - アクティブファイルのハイライト
  - ファイルクリック → エディターにロード
- [ ] ファイル切り替えロジック（未保存変更の検出と警告）
- [ ] キーボードショートカットの実装
  - `Ctrl+S`: 保存
  - `Ctrl+O`: フォルダを開く
  - `Ctrl+N`: 新規ファイル
  - `Ctrl+B`: サイドバーの表示/非表示
- [ ] サイドバーのリサイズ機能

**完了条件**: フォルダを開き、ファイルを選択・編集・保存できる完全なワークフローが動作する

---

## フェーズ 5: テストと仕上げ ✅
> **目標**: 品質を確保し、Windows 11 での動作を確認する

- [x] Windows 11 上での動作テスト
- [x] パフォーマンス測定（起動時間、タイピング遅延、メモリ使用量）
- [x] ネオブルータリズムのホバー/アクティブ状態の微調整
- [x] エッジケースの処理
  - 大きなファイル（> 1MB）の読み込み
  - 深いディレクトリ構造
  - 日本語を含むファイルパス
- [ ] アプリアイコンの設定
- [x] ビルドスクリプトの設定と `.exe` の生成テスト

**完了条件**: Windows 11 上でストレスなく使用でき、`.exe` が正常にビルドされる

---

## フェーズ 6: UI不具合修正とデザイン改善 ✅
> **目標**: DOM読み込みの不具合を解消し、デザイン規約（絵文字禁止）を徹底する

- [x] `index.html` の `<script>` 読み込み順序修正（DOMロード後実行）
- [x] UI上の絵文字（📂, 📝, ●など）の全面廃止
- [x] 代替として、汎用インラインSVGアイコン（Heroicons風）を導入

**完了条件**: スクリプトエラーが出ず、すべてのUI機能が正しく動作し、クリーンなSVGアイコンで統一される

---

## フェーズ 7: バグ修正とサイドバーUI再設計 ✅
> **目標**: Windowsダイアログ問題の回避と、高機能なVS Code風サイドバーの導入

- [x] `openFolder` 呼び出し時のRPCタイムアウト延長（10秒 → 30秒）
- [x] ダイアログエラー時のフォールバックとしてパス手動入力(`prompt`)対応
- [x] フォルダ未選択時の「新規ファイル」作成フローをフォルダ選択優先に改善
- [x] `createDirectory` RPCの追加（新規フォルダ作成用）
- [x] 2セクション構成の左サイドバーへ再設計（開いているエディタ + ワークスペース）
- [x] セクションヘッダーへのホバーアクション（SVGアイコン）追加
- [x] 複数ファイルの水平タブ管理機能（保存状態・閉じるボタン処理・ダーティ追跡）実装

**完了条件**: 複数のファイルを安定して同時に開き、タブで切り替えながら編集できる環境が整う

---

## フェーズ 8: フォルダ選択ダイアログの不具合修正
> **目標**: Windowsでネイティブフォルダ選択ダイアログが動作しない問題の調査・修正

- [x] `openFolder` にデバッグログを追加し、エラーの正体を特定する
- [x] フォールバックUXの改善（`prompt()` のデフォルトパスを `C:\Users` に変更）
- [ ] ネイティブダイアログが動作する場合の正常フロー確認

**完了条件**: フォルダを開く操作が安定して動作し、ファイルツリーが表示される

## フェーズ 9: UI名称変更 & Untitledファイルのサポート
> **目標**: 「開いているエディタ」の名称変更と、VS Code風の未保存ファイル作成フロー実現

- [x] UIセクション名の変更
- [x] 仮想パス(`Untitled-X`)による未保存ファイルのサポート
- [x] 未保存ファイル変更時の破棄確認ダイアログ実装

**完了条件**: フォルダに依存せず新規ファイルが作成でき、意図しない変更破棄が防げるようになる

---

## フェーズ 10: ネイティブSaveダイアログとUI名称改善
> **目標**: ファイル保存時やフォルダ作成時に、手動入力ではなくネイティブOSのダイアログを使用する

- [x] PowerShellを用いた非同期 `SaveFileDialog` の実装
- [x] PowerShellを用いた非同期 `FolderBrowserDialog` の実装
- [x] 「フォルダを開く」ボタンを「ワークスペースを開く」に名称変更

**完了条件**: `prompt()`による手動パス入力が撤廃され、すべてネイティブダイアログで操作できる状態になる

## フェーズ 11 & 12: ネイティブダイアログのパス不具合修正
> **目標**: PowerShellを利用したダイアログでの文字化けやパス指定不具合を修正する

- [x] PowerShell出力のUTF-8強制によるパスの文字化け（CP932問題）修正
- [x] `$`等の変数展開を防ぐためのシングルクォートエスケープ処理
- [x] SaveFileDialogでの初期パス（`InitialDirectory`）とファイル名（`FileName`）の分離

**完了条件**: 文字化けせず、またパスがファイル名欄に固定化されず直感的に保存できるようになる

---

## マイルストーン概要

| マイルストーン | 内容                                             | 優先度 |
| -------------- | ------------------------------------------------ | ------ |
| M1             | プロジェクト起動 (ElectroBun + React + Tailwind) | 🔴 最高 |
| M2             | エディターで Markdown が書ける                   | 🔴 最高 |
| M3             | ファイルとフォルダ機能の連携とUX改善             | 🔴 最高 |
| M4             | VS Code風 2段構成サイドバーUI                    | 🟡 高   |
| M5             | Windows ビルドと品質確保                         | 🟡 高   |
| M6             | フォルダ選択ダイアログのWindows対応              | 🟡 高   |

